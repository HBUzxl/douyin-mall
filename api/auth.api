
type PingReq {
}

type PingResp {
	Info string `json:"info"`
}

@server (
    group: auth
    prefix: api/v1
)

service atlas-api {
    @doc (
        summary: "test"
    )
    @handler ping
    get /auth/ping (PingReq) returns (PingResp)
}