syntax = "proto3";

package eino;
option go_package="./eino";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Eino {
  rpc Ping(Request) returns(Response);
}
